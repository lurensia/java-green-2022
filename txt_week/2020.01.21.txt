Node.js(자바스크립트 환경, 중요?)

파싱: 서로 프로토콜을 알아야한다.

*통신할때는 받는사람이 파싱을 통해 자신의 오브젝트로 변환해야 한다.*

JSON=파싱하는 메서드를 하나로 통일하기 위해 사용.
====================
URI = 리소스를 식별자로 찾아가는 방식

SDK =

===============
자바: 조건문, 반복문, 자료형(모르겠으면 책 한번만 읽어보기)

JVM아키텍쳐(그림못그리겠으면 물어보러 와라. 다시배워야한다.)

객체지향프로그램
-상태는 행위를 통해 변경한다.
-DIP 의존성 역전 원칙
-SRP - 월

String (메서드 활용)
equals() //==
추상화(밑에서 위로) / 인터페이스(위에서 밑으로) 강제성의 유무

-Json:Gson , Json2Pojo
-컬렉션(*ArrayList*, HashSet), 배열(x)
-메서드 return -> 모르면 공부

버블정렬 숙제로(bubble.sort).